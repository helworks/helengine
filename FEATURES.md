# Pending Features

- [ ] Component-to-component scene serialization: allow a serialized component to reference another component through the owning entity ID and stable component key.
- [ ] Move binary write serialization out of `helengine.core`, keeping write-side APIs and serializers in `helengine.files` while Core remains runtime/read-side only.
