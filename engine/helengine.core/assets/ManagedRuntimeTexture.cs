namespace helengine {
    /// <summary>
    /// Represents a lightweight runtime texture instance used when build tooling needs texture metadata without GPU state.
    /// </summary>
    public sealed class ManagedRuntimeTexture : RuntimeTexture {
    }
}
