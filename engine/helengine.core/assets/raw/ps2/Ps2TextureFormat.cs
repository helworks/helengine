namespace helengine {
    /// <summary>
    /// Identifies the PS2-native runtime texture payload format produced by the PS2 builder.
    /// </summary>
    public enum Ps2TextureFormat : byte {
        /// <summary>
        /// Stores texels as 32-bit RGBA values for the initial PS2 runtime texture path.
        /// </summary>
        Rgba32 = 0
    }
}
