namespace helengine.editor.tests.testing {
    /// <summary>
    /// Deterministic enum used to verify supported enum dictionary keys in automatic persistence tests.
    /// </summary>
    public enum TestDictionaryMode {
        /// <summary>
        /// Primary scene mapping mode.
        /// </summary>
        Primary = 1,

        /// <summary>
        /// Secondary scene mapping mode.
        /// </summary>
        Secondary = 2
    }
}
